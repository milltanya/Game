#ifndef GAME_PARSER_H
#define GAME_PARSER_H

#include <ctime>
#include <iostream>
#include <string>
#include <vector>

class CStringParse {
public:
    std::vector<std::string> parse(const std::string& s);
};

#endif
